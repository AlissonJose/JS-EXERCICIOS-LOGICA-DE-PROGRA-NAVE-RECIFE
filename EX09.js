const read = require("readline-sync")
    var larg = 15
    var comp = 20
    var terreno = larg*comp;
    var casa = 9*12
        console.log(`O terreno possui: ${terreno}m²`)
    var calcTC = parseFloat (terreno-casa)
        console.log (`A residencia possui: ${casa}m² e resta do terreno ${calcTC}m²`)


